import os
# Permite acessar variáveis de ambiente do sistema e arquivos( como o .env)

import requests
# Biblioteca usada para fazer requisições HTTP (chamadas de API)

from dotenv import load_dotenv
# Carrega variáveis do arquivo ".env" para o ambiente Python.

import time
# Para evitar exxeceder o limite da API.